name: Build AITL Space Paper  

on:
  workflow_dispatch: {}   # ← 手動実行ボタンだけ出す

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from AITL Space workflow"
